Htm programs:
----------------

C-programs
----------------

- HTM_INDEX: Return triangle leaf ids for input ra,dec list
  
  id =  htm_index(ra, dec, depth) ; ra,dec may be arrays in degrees

- HTM_INTERSECT: Return triangles that intersect circle centered at ra,dec
                 radius angle
  idlist = htm_intersect(ra, dec, depth, angle, status=)
                 Angle in radians, ra,dec, degrees

IDL programs
---------------

htm_match, ra1, dec1, ra2, dec2, angle, match1, match2, d12, 
                    htmid2=, htmrev2=, minid=, maxid=, depth=, status=
ra/dec in degrees, double precision
angle in radians.  May be an array of same length as ra1,dec1
status=0 is success, status=2 no matches, otherwise failure
